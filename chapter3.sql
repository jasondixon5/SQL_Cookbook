/*

SELECT ENAME, DEPTNO
FROM EMP
WHERE DEPTNO IN (10)

UNION ALL

SELECT "-----", "------"

UNION ALL

SELECT DNAME, DEPTNO
FROM DEPT
;

SELECT EMP.ENAME, DEPT.LOC
FROM EMP, DEPT
WHERE EMP.DEPTNO IN (10)
    AND DEPT.DEPTNO = EMP.DEPTNO
;

SELECT EMP.ENAME, DEPT.LOC
FROM EMP
INNER JOIN DEPT
ON DEPT.DEPTNO = EMP.DEPTNO
WHERE EMP.DEPTNO IN (10)
;
-- 3.3
CREATE VIEW V
AS
SELECT ENAME, JOB, SAL
FROM EMP
WHERE JOB = "CLERK"
;

SELECT * FROM V;

SELECT V.ENAME, V.JOB, V.SAL, E.EMPNO, E.MGR, E.HIREDATE, E.COMM, E.DEPTNO
FROM V
JOIN EMP E
ON (
    E.ENAME = V.ENAME
    AND E.JOB = V.JOB
    AND E.SAL = V.SAL
    )
;
*/
-- 3.4
SELECT * FROM DEPT
;
SELECT "----"
;
SELECT DEPTNO FROM EMP
;

SELECT DISTINCT DEPTNO FROM EMP;


SELECT DEPTNO 
FROM DEPT
WHERE DEPTNO NOT IN (20)
;

SELECT DEPTNO 
FROM DEPT
WHERE DEPTNO NOT IN (SELECT 20)
;

SELECT DEPTNO 
FROM DEPT
WHERE DEPTNO NOT IN (
    SELECT DISTINCT DEPTNO FROM EMP
    )
;

that, by default,

Molinaro, Anthony. SQL Cookbook: Query Solutions and Techniques for Database Developers (Cookbooks (O'Reilly)) (p. 33). O'Reilly Media. Kindle Edition. 